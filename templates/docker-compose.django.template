networks:
  proxy_sql-network:
    external: true

services:
  $1-service:
    build: /src/$1/web
    container_name: $1
    hostname: $1
    networks:
      - proxy_sql-network
    volumes:
      - /src/$1/web:/src/$1/
      - /static/$1:/static/$1
      - /media/$1:/media/$1
      - /var/log:/var/log
    environment:
      DB_HOST: ${DB_HOST}
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      KC_SECRET: ${KC_SECRET}
      DJANGO_SECRET: ${DJANGO_SECRET}
    restart: unless-stopped
